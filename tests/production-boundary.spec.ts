import{test,expect}from'@playwright/test';
test.beforeEach(async({page})=>{await page.addInitScript(()=>sessionStorage.setItem('aiai:intro-seen','yes'));await page.route('**/api/v1/session',route=>route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({subject:null,tier:'free',roles:[],expiresAt:null})}));});
test('生产界面保留正式账号弹窗并移除演示业务',async({page,request})=>{await page.goto('/account/login?returnTo=%2F%23home');await expect(page.getByRole('dialog')).toBeVisible();await expect(page.getByPlaceholder('请输入账号')).toBeVisible();await expect(page.getByText(/测试用户|隔离沙盒|可审阅预览|签名密钥|回调地址/)).toHaveCount(0);await page.goto('/play/drama-05');await expect(page.getByRole('heading',{name:'该集暂时无法播放'})).toBeVisible();await expect(page.locator('video')).toHaveCount(0);await page.goto('/admin/moderation');await expect(page.getByText('没有找到这个页面')).toBeVisible();const response=await request.get('/media/demo/landscape.mp4');expect(response.headers()['content-type']).toContain('text/html');});
