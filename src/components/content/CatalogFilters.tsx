import {genres} from '../../data/dramas';
export function FilterTags({label,options,value,onChange}:{label:string;options:readonly (readonly [string,string])[];value:string;onChange:(value:string)=>void}){return <div className="tag-filter-row"><span className="tag-filter-label">{label}</span><div className="tag-options" role="group" aria-label={label}>{options.map(([id,name])=><button key={id} type="button" aria-pressed={value===id} onClick={()=>onChange(id)}>{name}</button>)}</div></div>;}
export function CatalogFilters({genre,update,sort,onChange,onReset}:{genre:string;update:string;sort:string;onChange:(key:string,value:string)=>void;onReset:()=>void}){return <section className="catalog-tag-filters surface-panel" aria-label="作品筛选"><FilterTags label="题材" options={genres.map(g=>[g,g])} value={genre} onChange={v=>onChange('genre',v)}/><FilterTags label="更新状态" options={[["all","全部状态"],["ongoing","更新中"],["completed","已完结"],["unknown","待公布"]]} value={update} onChange={v=>onChange('update',v)}/><FilterTags label="排序" options={[["curated","推荐顺序"],["title","按标题"]]} value={sort} onChange={v=>onChange('sort',v)}/><button className="text-button clear-tags" onClick={onReset}>清除条件</button></section>;}
