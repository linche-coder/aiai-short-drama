# 爱爱短剧持续开发进度

更新：2026-09-17

## 登录注册弹窗与正式界面修订

- 2026-09-17 个人中心修订：各子页面统一使用同宽内容容器，切换“概览 / 追剧 / 历史 / 订单 / 评论 / 消息 / 资料 / 隐私”时导航宽度不再变化；个人资料页增加头像更换与恢复默认，未上传时显示用户名首字母。
- 2026-09-17 会员中心：会员点击页头“会员”或个人中心“会员中心”均进入正式会员中心，展示当前会员、有效期、V3 等级与成长进度、实际已实现权益、订单入口和续费方案；免费用户继续看到会员方案页。
- 2026-09-17 账号视觉修订：登录后页头仅显示头像入口，不再展示预览账号昵称；免费用户使用深色细描边头像，会员使用粉紫渐变光环与皇冠角标。个人中心显示实际登录账号及权益等级，不再出现“免费体验账号 / 会员体验账号”。
- 2026-09-17 预览账号：本机开发服务器新增免费与高级会员各一个隔离账号，通过正式登录接口和 HttpOnly 会话 Cookie 工作；凭据保存在被 Git 忽略且禁止静态访问的 `.env.local`，生产包不包含账号或密码。
- 2026-09-17 复验修订：删除登录/注册弹窗的重复标题；受限剧集提示删除底部“关闭”按钮，并修复“开通会员”与“暂不观看”按钮间距。构建及账号弹窗回归 7/7 通过。
- `/account/login`、`/account/register`、`/account/forgot-password` 已改为覆盖原页面的共享账号弹窗；支持关闭按钮、Escape、点击遮罩、焦点循环、背景滚动锁定、浏览器返回和安全站内回跳。
- 登录表单已按账号/密码结构实现；注册改为账号、密码、确认密码、选填邮箱。密码不会写入 URL、浏览器存储或日志。
- 删除普通用户界面的测试用户 A/B、沙盒注册、支付回调模拟和开发验收文案。认证、注册、找回、评论、反馈、订单均只在真实 HTTP 接口成功后显示成功状态。
- 会员和收银台改为正常产品信息；支付服务未接通时显示简洁不可用状态，不创建假订单或假权益。
- 播放页不再加载彩条视频；卡片不再把演示媒体标记为可播放。无正式片源时显示“该集暂时无法播放”。
- 受限集会员提示会携带作品和目标集数进入会员页、收银台；不会因登录自动付款。
- 评论草稿在打开登录弹窗时保留，提交按钮改为“发表评论”；反馈按钮改为“提交反馈”，失败不会伪报成功。
- 个人中心子页面增加统一导航，空状态、帮助中心、资料、消息、历史与政策页面已改为产品文案。
- 本轮截图、测试覆盖与依赖状态见 `docs/auth-modal/DELIVERY.md`。

## 已完成

- 保留 React 19 + TypeScript + Vite、静态 Sites 部署、现有路由、暗色粉紫品牌和首页轮播。
- 核实正式 `videoSources` 为空；两个 12 秒测试视频为 H.264/AAC 中性色条。开发预览逐集映射与正式媒体完全分离，生产构建删除测试媒体目录。
- 移除播放页“免费/基础/高级”自选控件；权益改由隔离账号或订单沙盒状态提供。免费 1–6、基础 1–30、高级 1–36 仅用于开发验收。
- 新增登录、注册、找回密码、验证/重置错误、会话失效和可信站内回跳页面；测试密码不持久化，生产未接认证时不开放测试身份。
- 游客收藏改为设备持久化；登录时如存在游客收藏，必须明确选择合并或保留，不静默覆盖。
- 新增会员当前状态、订单确认、订单列表、订单详情、支付/权益结果；沙盒覆盖成功、失败、取消和延迟发权，成功回调幂等，跨账号订单读取被拒绝。
- 新增分集评论、回复、点赞、剧透折叠、举报、作者删除、我的评论、回复站内通知；没有预置虚构评论或热度。
- 新增我的追剧、观看历史/继续观看、资料、消息中心、帮助、反馈、工单和站点信息草稿页。
- 新增开发环境最小后台：评论/举报审核、工单处理；沿用本地角色门槛，生产构建不包含后台演示模块。
- 扩展真实 HTTP 接口契约；浏览器沙盒能力继续明确标为演示，不宣称服务端安全已经上线。
- 新增 390/768/1440 响应式样式和关键页面截图。

## 验证证据

- `npm.cmd run build`：通过；生产 `dist` 不包含 `media/demo`。
- `$env:PW_TEST_CHANNEL='chrome'; npx.cmd playwright test tests/preview-accounts.spec.ts`：2/2 通过；覆盖免费/会员正式登录、默认头像、上传/恢复头像、子页面导航同宽、会员中心桌面与手机布局。
- `$env:PW_TEST_CHANNEL='chrome'; npx.cmd playwright test tests/auth-modal.spec.ts`：7/7 通过。
- `$env:PW_TEST_CHANNEL='chrome'; npx.cmd playwright test closure.spec.ts`：5/5 通过。
- 覆盖：可信回跳、播放页无权益自选、订单幂等发权、跨账号订单隔离、评论回复/点赞/通知/删除边界、游客收藏显式合并、390/768/1440 无横向溢出。
- 截图：`docs/closure/login-1440.png`、`checkout-1440.png`、`comments-1440.png`、`me-1440.png`、`mobile-checkout-390.png`。

## 外部阻塞

- 认证：缺身份提供方、邮件发送、Cookie 会话、CSRF、设备/注销服务。
- 支付：缺商户号、支付 SDK、签名密钥、回调地址、退款接口和正式商业规则。
- 媒体：缺有权使用的正片、逐集稳定 ID/标题/时长/字幕/清晰度及 CDN 授权。
- 持久化：缺服务端数据库、对象存储、行级隔离、管理 RBAC 与审计日志。
- 头像：当前预览在本设备压缩并保存头像；跨设备同步、服务端文件校验与对象存储仍需后端服务。
- 通知：缺邮件/短信/浏览器推送通道；当前只有本地沙盒站内消息。
- 运营：缺经营主体、联系方式、资质、退款政策、升级算法、内容排期与真实榜单指标。

## 下一步

剩余工作均需要上述外部依赖：选定认证与数据服务，落地迁移/RLS；接入真实媒体授权；配置支付沙盒并做签名回调验收；提供经营与会员规则后定稿协议。未获得授权前不发布生产、不发通知、不扣款。
