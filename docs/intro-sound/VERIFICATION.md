# V4：仅保留 Logo 出现音效

当前使用 aiai-intro-reveal-v4.wav，取自原始 V2 开头 1.15 秒，原速播放，末尾 300ms 淡出。动画开始时同步播放，仅为 Logo 出现配音，后续定格、飞行和落位保持安静。动画本身不变。重建脚本：scripts/render-intro-reveal.mjs。

以下 V3 为历史编排，已不再使用。

# V3：按 Logo 动作重新编排音频

当前使用 aiai-intro-logo-synced-v3.wav，长度 3.861995 秒，与 3.862 秒动画对齐，原始 V2 文件保留。

- 0.14–1.55 秒：爱心显现音，保留渐入和收束。
- 0.90–1.55 秒：字标展开的过渡音，与显现音柔和叠加。
- 1.55 秒附近：完整 Logo 定格重音。
- 1.962–3.112 秒：单独处理的飞行音，随 Logo 向顶栏移动。
- 3.112–3.862 秒：落位尾音，淡出至动画结束。

由原始素材分段、保持音高调整节奏、交叉叠加与淡入淡出生成，并进行峰值限制。播放延迟改为 0，与动画共享时间零点；加载、缓冲或交互解锁后校准播放位置。未改变动画时间。

重建：node scripts/render-intro-sound.mjs（需要 FFmpeg），滤镜编排见 scripts/intro-sound-sync-filter.txt。

验证：3 项浏览器检查通过；真实播放抽样 75 次，最大媒体时间与动画时间差约 70.3ms（浏览器媒体时间，不包含外接设备延迟）。记录见 sync-v3.json。首次访问浏览器禁止自动有声播放时，需要开屏期间点击或按键解锁；结束后不会补播。

---

## V2 历史记录（以下音频延迟设置已被 V3 替代）

# 品牌入口、单排推荐与开屏音效

- 品牌组合拆为两个平级链接，无嵌套链接：爱爱短剧 → /#home，18+专区 → /18plus。
- 精选推荐固定完整一排：大屏 4 张、中屏 3 张、手机 2 张。最近上新排除当前推荐的作品。
- 原始音频复制为 public/assets/audio/aiai-intro-cinematic-v2.wav，SHA-256 与用户提供文件相同，未剪辑、变速或覆盖源文件。PCM 双声道，44.1 kHz、16 bit，时长 2.6 秒。
- 保留动画原始时间：出现 1550ms、停留 412ms、离场 1900ms、飞行 1150ms，总时长 3862ms。音频延迟 162ms 开始，以音频约 1.8 秒的重音对应动画 1962ms 的飞行起点，音量 65%。
- 首次完整开屏时播放；减少动态效果模式不播放。音频加载较晚或开屏期间用户交互解锁播放时，从当前动画对应的音频位置开始。后台隐藏、动画结束或组件卸载时停止，结束后不补播。
- 首次访问的自动有声播放受浏览器策略限制；无法自动播放时，开屏期间点击或按键会尝试解锁，不影响动画完成。刷新沿用原首次展示逻辑，不重复开屏或播放声音。

验证：构建通过，3 项自动检查通过，包含五种宽度下的一排卡片与分离链接、实际播放成功、刷新不重播、自动播放拒绝、减少动态效果和动画结束后不补播。
