{
  "name": "aiai-line-entrance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "build:release": "node scripts/build.mjs --release",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
